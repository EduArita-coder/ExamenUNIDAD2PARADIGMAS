info:
  name: Edit
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:5194/api/simulations/1
  body:
    type: json
    data: |-
      {
        "montoInicial": 200,
        "tasaInteresAnual": 0.010,
        "plazoAnios": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
